<UserControl x:Class="WPFBiblioteca.Views.TablesViews.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFBiblioteca.Views.TablesViews"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="845"
             d:DesignWidth="1535">
    <Border
        Background="{StaticResource primarySolidColor}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBox Style="{StaticResource ModernTextbox}"
                         Margin="25,25,25,25"
                         Width="auto"
                         HorizontalAlignment="Left"/>
            </StackPanel>
            <StackPanel Grid.Row="1">
                <TextBlock 
                    Style="{StaticResource ViewsHeaders}"
                    Text="Bienvenid@"
                    Margin="25,15,25,25"
                    FontSize="48"
                    Width="auto"
                    HorizontalAlignment="Left"/>
            </StackPanel>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <StackPanel Orientation="Vertical">
                        <Button
                            Margin="50,15,50,10"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            BorderThickness="0"
                            Padding="0">

                            <Button.Template>
                                <ControlTemplate>
                                    <Grid>
                                        <Border 
                                            BorderThickness="2"
                                            CornerRadius="50"
                                            Height="280"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch">
                                            <Border.Background>
                                                <ImageBrush Stretch="UniformToFill" ImageSource="/Images/lend-a-book.jpg"/>
                                            </Border.Background>
                                            <Border.Effect>
                                                <DropShadowEffect 
                                                    BlurRadius="10"
                                                    Opacity=".5"
                                                    ShadowDepth="10"
                                                    Color="Gray" />
                                            </Border.Effect>
                                        </Border>
                                        <Border Background="{StaticResource HomeLendReturnGradient}" CornerRadius="50"/>
                                        <Grid VerticalAlignment="Bottom">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid Grid.Column="0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                     <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock 
                                                    Grid.Row="0"
                                                   Margin="20,0,20,0"
                                                   Style="{StaticResource LendReturnTextStyle}"
                                                   FontSize="46"
                                                   Text="Realizar un prestamo"/>
                                                <TextBlock 
                                                    Grid.Row="1"
                                                   Margin="50,0,20,20"
                                                   FontWeight="Light"
                                                   FontSize="16"
                                                   Style="{StaticResource LendReturnTextStyle}"
                                                   Text="Seleccione para realizar el registro de un prestamo"/>
                                            </Grid>
                                            <fa:IconImage
                                                Grid.Column="1" 
                                                Margin="20"
                                                VerticalAlignment="Top"
                                                Icon="plus" 
                                                Foreground="{StaticResource primarySolidColor}" />
                                        </Grid>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button
                            Margin="50,10,50,50"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            BorderThickness="0"
                            Padding="0">

                            <Button.Template>
                                <ControlTemplate>
                                    <Grid>
                                        <Border 
                                            BorderThickness="2"
                                            CornerRadius="50"
                                            Height="280"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch">
                                            <Border.Background>
                                                <ImageBrush Stretch="UniformToFill" ImageSource="/Images/return-a-book.jpg"/>
                                            </Border.Background>
                                            <Border.Effect>
                                                <DropShadowEffect 
                                                    BlurRadius="10"
                                                  Opacity=".5"
                                                  ShadowDepth="10"
                                                  Color="Gray" />
                                            </Border.Effect>
                                        </Border>
                                        <Border Background="{StaticResource HomeLendReturnGradient}" CornerRadius="50"/>
                                        <Grid VerticalAlignment="Bottom">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid Grid.Column="0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock 
                                                    Grid.Row="0"
                                                   Margin="20,0,20,0"
                                                   Style="{StaticResource LendReturnTextStyle}"
                                                   FontSize="46"
                                                   Text="Devolver un prestamo"/>
                                                <TextBlock 
                                                    Grid.Row="1"
                                                   Margin="50,0,20,20"
                                                   FontWeight="Light"
                                                   FontSize="16"
                                                   Style="{StaticResource LendReturnTextStyle}"
                                                   Text="Seleccione para añadir la devolución de un libro prestado"/>
                                            </Grid>
                                            <fa:IconImage
                                                Grid.Column="1" 
                                                Margin="20"
                                                VerticalAlignment="Top"
                                                Icon="plus" 
                                                Foreground="{StaticResource primarySolidColor}" />
                                        </Grid>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>


                    </StackPanel>
                </Grid>
                <Grid Grid.Column="1">
                    <Border Background="Gray"
                            CornerRadius="50"
                            Margin="25">


                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
